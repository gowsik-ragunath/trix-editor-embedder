<nav class="flex items-center justify-between flex-wrap bg-red-500 p-6">
  <div class="flex items-center flex-shrink-0 text-white mr-6">
    <span class="font-semibold text-xl tracking-tight">Project</span>
  </div>
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
    <div class="text-sm lg:flex-grow">
      <%= link_to "Home", root_path, class: "block mt-4 lg:inline-block lg:mt-0 text-white-200 hover:text-white mr-4" %>
      <%= link_to "Rooms", rooms_path, class: "block mt-4 lg:inline-block lg:mt-0 text-white-200 hover:text-white mr-4" %>
    </div>
    <% if user_signed_in? %>
	    <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "block mt-4 lg:inline-block lg:mt-0 text-white-200 hover:text-white mr-4" %>
  	<% end %>
  </div>
</nav>